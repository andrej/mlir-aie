It seems the agents working on the reconfigure_loadpdi_xclbin example are a bit hung up on the load_pdi instruction. They should use the higher-level aiex.configure and aiex.run instructions and break the example into a @main device configuration (aie.device @main) and then the two actual devices @add_two and @add_three. The materialize-runtime-sequences pass will lower those aiex.configure instructions into load_pdi instructions later, but we do need to ensure the higher-level functionality using aiex.configure also works with the new xclbin flow.
